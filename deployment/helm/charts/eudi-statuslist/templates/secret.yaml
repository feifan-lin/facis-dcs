apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-eudi-statuslist
  labels:
    app.kubernetes.io/name: eudi-statuslist
    app.kubernetes.io/instance: {{ .Release.Name }}
type: Opaque
stringData:
  API_key: {{ required "eudiStatuslist.apiKey is required" .Values.apiKey | quote }}
